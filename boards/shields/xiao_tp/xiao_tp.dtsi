/*
 * XIAO TrackPoint Only Shield
 * Minimal device tree for TrackPoint-only device
 */

/ {
    chosen {
        // TrackPoint-only device - no keyboard matrix needed
        // The PS/2 mouse will be configured in the overlay
    };
};
